# SQUEEZE System Architecture

This document details the architectural design, network topology, discovery mechanics, and lifecycle state machines of SQUEEZE.

---

## 1. System Topology & Component Overview

SQUEEZE is composed of two primary subsystems:
1. **`SQUEEZE_SERVER`**: A lightweight Go daemon orchestrating FFmpeg encoding pipelines, hardware discovery, job queue management, and chunked streaming transfers.
2. **`SQUEEZE_UI`**: A cross-platform Avalonia MVVM application targeting desktop (.NET 10 on Linux/macOS/Windows) and mobile (Android).

```mermaid
flowchart TD
    subgraph Client["SQUEEZE Client (Avalonia MVVM)"]
        UI["Reactive UI / Views"]
        VM["MainViewModel & State"]
        DISC["mDNS / Subnet Discovery"]
        NET["Transfer & API Engine"]
    end

    subgraph Network["Local Area Network / Wi-Fi Hotspot"]
        MDNS["mDNS Multicast (224.0.0.251:5353)"]
        BCAST["Directed Subnet Broadcast (e.g. 192.168.43.255:5353)"]
        SWEEP["Direct HTTP Health Sweep (:8080)"]
    end

    subgraph Server["SQUEEZE Server (Go Daemon)"]
        ROUTE["HTTP Router & Auth Guard"]
        QUEUE["Job Pool & Priority Queue"]
        PROBE["Hardware Probe & Caching"]
        RUNNER["FFmpeg Process Runner"]
        STORE["Scratch & Output Storage"]
    end

    UI --> VM
    VM --> NET
    VM --> DISC
    DISC -.-> MDNS
    DISC -.-> BCAST
    DISC -.-> SWEEP
    MDNS -.-> ROUTE
    BCAST -.-> ROUTE
    SWEEP -.-> ROUTE
    NET -->|REST API & SSE Events| ROUTE
    ROUTE --> QUEUE
    QUEUE --> STORE
    QUEUE --> RUNNER
    PROBE --> RUNNER
```

---

## 2. Network Discovery Architecture

Zero-configuration discovery operates across heterogeneous networks, including enterprise LANs, home Wi-Fi routers, and Android mobile hotspots (which frequently drop standard multicast packets).

### Multi-Vector Discovery Protocol

```mermaid
sequenceDiagram
    autonumber
    participant C as SQUEEZE Client
    participant N as LAN / Hotspot Router
    participant S as SQUEEZE Server

    Note over S: Server boots, probes hardware, starts mDNS advertiser
    S->>N: Register "_squeeze._tcp.local" (A & TXT records: ip=10.217.170.39, node_id=RIG_ALPHA_01)

    Note over C: User initiates discovery or auto-scans
    par Vector 1: Multicast mDNS
        C->>N: Multicast Query to 224.0.0.251:5353
        N-->>S: Forwarded (standard Wi-Fi)
        S-->>C: mDNS Response with TXT (ip=, node_id=, gpu=)
    and Vector 2: Directed Subnet Broadcast
        C->>N: Directed UDP Broadcast to Subnet (e.g. 192.168.43.255:5353)
        N-->>S: Broadcast Delivered (Mobile Hotspot fallback)
        S-->>C: mDNS Response with TXT
    and Vector 3: Background HTTP Sweep
        C->>S: GET http://{ip}:8080/api/v1/health
        S-->>C: 200 OK + Health JSON payload
    end

    Note over C: Client validates node reachability & registers server in UI
```

### Discovery Safety & RFC 1918 Address Filtering
- **Private IP Validation**: Cellular gateway/carrier addresses (e.g. `192.0.0.1`, `192.0.0.4` generated by CLAT / 464XLAT) and point-to-point tunnels (`net.FlagPointToPoint`) are strictly ignored.
- **Outbound IP Priority**: The server determines its primary private IPv4 via UDP routing table lookup (`net.DialUDP("udp4", nil, 8.8.8.8:80)`) and advertises this primary address in TXT records (`ip=...`).
- **Health Verification Probe**: Before presenting any node in the client UI, the client performs an HTTP handshake against `/api/v1/health`.

---

## 3. Job Lifecycle & State Machine

Every encode job transitions through strict deterministic states:

```mermaid
stateDiagram-v2
    [*] --> Created: POST /api/v1/jobs
    Created --> Uploading: Client begins streaming media
    Uploading --> Uploading: Chunked / Resumed stream
    Uploading --> Queued: Upload completed (automatic transition)
    Queued --> Processing: Worker available in JobPool
    
    state Processing {
        [*] --> Running
        Running --> Paused: POST /api/v1/jobs/{id}/pause (SIGSTOP)
        Paused --> Running: POST /api/v1/jobs/{id}/resume (SIGCONT)
    }

    Processing --> Completed: FFmpeg process exits 0
    Processing --> Error: Non-zero exit or abort
    Uploading --> Error: Incomplete upload / timeout
    
    Created --> Cancelled: DELETE /api/v1/jobs/{id}
    Queued --> Cancelled: DELETE /api/v1/jobs/{id}
    Processing --> Cancelled: DELETE /api/v1/jobs/{id} (SIGKILL)

    Completed --> [*]: Retention sweep (e.g. 24h)
    Error --> [*]: Retention sweep
    Cancelled --> [*]: Scratch storage purged
```

---

## 4. Hardware Acceleration & Encoder Probing

On startup, `SQUEEZE_SERVER` probes the physical host and caches verified hardware encoders.

```mermaid
flowchart TD
    Start([Server Boot]) --> CheckCache{Cache file valid &<br/>signature matches?}
    CheckCache -->|Yes| LoadCache[Load Cached Encoders & Status]
    CheckCache -->|No| ComputeSig[Compute Signature: CPU + GPU + FFmpeg]
    
    ComputeSig --> QueryFFmpeg[Run ffmpeg -encoders]
    QueryFFmpeg --> FilterCandidates[Filter HW Candidates: _vaapi, _nvenc, _qsv, _amf]
    
    FilterCandidates --> DryRun[Execute 1-frame dummy dry-run per candidate]
    DryRun --> VerifySuccess{Initialization<br/>succeeded?}
    VerifySuccess -->|Yes| MarkActive[Mark Active Hardware Encoder]
    VerifySuccess -->|No| MarkInactive[Mark Inactive / Software Fallback]
    
    MarkActive --> SaveCache[Write hardware_cache.json]
    MarkInactive --> SaveCache
    SaveCache --> Serve[Serve API & Advertise Capabilities via mDNS]
    LoadCache --> Serve
```

---

## 5. Client MVVM Reactive Architecture

The client UI is developed using [Avalonia UI](https://avaloniaui.net/) with compiled bindings and thread marshaling:
- **`Dispatcher.UIThread` Enforcement**: All asynchronous network transfers, mDNS event emissions, and SSE event handlers marshal property changes to the UI thread.
- **Responsive Adaptive Breakpoints**: Automatically adapts between widescreen desktop multi-column layouts and compact single-column mobile viewports via collapsible drawers and sidebars.
- **Storage Staging**: On Android, content URIs are safely staged to application cache storage (`Context.CacheDir`) before transcode initiation.

